---
name: 出演時間の特殊表記
description: ライブ出演時間を部別スケジュールへ分解する際の、意味を持つ特殊表記の扱い
---

「1部のみ」「2部だけ」のような表記は、部別スケジュールの見出しではなく、出演時間そのものを示す値として扱う。

**Why:** 部番号パターンだけで分解すると、`1部` が見出しへ移動し、画面には後続の「のみ」「だけ」だけが残る。

**How to apply:** 出演時間を部別にパースする前に、部名＋「のみ／だけ」の値全体を検出して、元の出演時間ラベルと全文を1行として表示する。