name: Install site from ZIP

on:
  push:
    paths:
      - "churuchubu-replit-complete-deploy.zip"
      - ".github/workflows/unpack-site.yml"

permissions:
  contents: write

jobs:
  unpack:
    if: "!contains(github.event.head_commit.message, '[skip ci]')"
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Unpack complete Replit site
        shell: bash
        run: |
          set -euo pipefail

          rm -rf _site
          mkdir -p _site
          unzip -q "churuchubu-replit-complete-deploy.zip" -d _site

          # Remove old repository contents, but keep Git metadata,
          # the workflow, the source ZIP, and the temporary extraction directory.
          find . -mindepth 1 -maxdepth 1             ! -name '.git'             ! -name '.github'             ! -name 'churuchubu-replit-complete-deploy.zip'             ! -name '_site'             -exec rm -rf {} +

          # Copy the complete extracted application into repository root.
          cp -a _site/. .
          rm -rf _site

          # Do not publish the deployment helper ZIP.
          rm -f "churuchubu-replit-complete-deploy.zip"

      - name: Commit extracted site
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "No changes to commit."
            exit 0
          fi
          git commit -m "Deploy complete Replit site [skip ci]"
          git push
